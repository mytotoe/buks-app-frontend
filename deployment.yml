apiVersion: apps/v1
kind: Deployment
metadata:
  name: pipeline-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pipeline-app
  template:
    metadata:
      labels:
        app: pipeline-app
    spec:
      containers:
      - name: myapp
        image: japhetism/pipeline-app
        imagePullPolicy: Always
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
        ports:
        - containerPort: 80



---


apiVersion: v1
kind: Service
metadata:
  name: pipeline-app
spec:
  type: NodePort
  selector:
    app: pipeline-app
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    nodePort: 31000